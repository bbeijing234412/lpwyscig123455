services:
  - type: web
    name: lp-race-tracker
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: RIOT_API_KEY
        sync: false
      - key: POLL_INTERVAL_MINUTES
        value: "5"
